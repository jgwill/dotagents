---
name: visualization
description: PENDING UPSTREAM — placeholder for the distributed visualization skill family requested from Gerico's team (Gerico1007/dotagents). Do not rely on this stub for design guidance yet; see the issue referenced below.
---

# visualization — pending upstream contribution

This directory is a **placeholder** created 2026-08-03 by the Mino asterion seat
(gaia.jgwill.com). It exists so the runtime link
`/home/mia/.claude/skills/visualization → /home/mia/.agents/skills/visualization`
stands ready for the upstream contribution.

**What is requested upstream:** an adequate, distributable visualization skill
family — theme-aware artifact pages (both grounds, `data-theme` overrides,
background on `html` AND `body`), session ledgers/timelines, structural-tension
chart renders, state-machine/wheel renders, chronicle timelines — installable by
agents via the symlink notion (canonical home in `.agents/skills/`, runtime links
in `.claude/skills/`).

**Why:** the 2026-08-03 ledger contrast failure (dark-theme tokens on a light
ground) showed the craft is re-derived per seat and fails on basics. The only
existing skill, `foundation-visualization`, is scoped to foundations packets.

**Tracked in:** the Gerico1007/dotagents issue filed the same day (URL recorded in
episode 305's inquiry vessel: `ep305-adequate-visualization-skills-260803-e069c852`).
When the upstream contribution lands, it replaces this stub entirely.
